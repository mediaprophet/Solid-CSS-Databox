apiVersion: v1
kind: Service
metadata:
  name: databox-ipms
  labels:
    app.kubernetes.io/name: databox-ipms
spec:
  selector:
    app.kubernetes.io/name: databox-ipms
  ports:
    - name: http
      port: 80
      targetPort: http
