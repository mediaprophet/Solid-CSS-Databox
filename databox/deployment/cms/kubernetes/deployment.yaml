apiVersion: apps/v1
kind: Deployment
metadata:
  name: databox-cms
  labels:
    app.kubernetes.io/name: databox-cms
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: databox-cms
  template:
    metadata:
      labels:
        app.kubernetes.io/name: databox-cms
      annotations:
        databox.org/device-tls: devices host requires direct TLS or verified edge client-cert forwarding
    spec:
      containers:
        - name: community-solid-server
          image: solid-databox:local
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -c
            - |
              if [ ! -s /run/secrets/cms_control_token ]; then
                echo "Missing /run/secrets/cms_control_token; create the cms-control-token Secret." >&2
                exit 64
              fi
              export CSS_CMS_CONTROL_TOKEN="$(cat /run/secrets/cms_control_token)"
              exec node bin/server.js
          envFrom:
            - configMapRef:
                name: databox-cms-config
          ports:
            - name: http
              containerPort: 3000
          volumeMounts:
            - name: cms-data
              mountPath: /data
            - name: cms-control-token
              mountPath: /run/secrets
              readOnly: true
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
      volumes:
        - name: cms-data
          persistentVolumeClaim:
            claimName: databox-cms-data
        - name: cms-control-token
          secret:
            secretName: cms-control-token
            items:
              - key: cms-control-token
                path: cms_control_token
